# `@cloxa/database`

Supabase-generated database types and database-specific helpers belong here. Package
intentionally contains no schema, query, or migration code yet. Next.js request clients
stay in `apps/web` because they depend on request cookies.
